import { ConfirmDialog } from '../../../../../../../shared/ui';
import type { WorkflowComponentsController } from '../../_hooks/useWorkflowComponents';
export function ComponentConfirmations({ c }: {
    c: WorkflowComponentsController;
}) { return <><ConfirmDialog open={c.confirmLeaveEditor} title="خروج از ویرایش کامپوننت" message="تغییرات این نسخه هنوز ذخیره نشده‌اند. خروج، تغییرات ویرایشگر کامپوننت را دور می‌ریزد." confirmLabel="خروج بدون ذخیره" danger onClose={() => c.setConfirmLeaveEditor(false)} onConfirm={c.leaveEditor}/><ConfirmDialog open={Boolean(c.pendingUpgrade)} title="ارتقای نمونه کامپوننت" message={c.pendingUpgrade ? `نسخه ${c.pendingUpgrade.version.semantic_version} ساخته شد. آیا نمونه این کامپوننت در جریان جاری به نسخه جدید ارتقا یابد؟ جریان‌های دیگر بدون تغییر می‌مانند.` : ''} confirmLabel="ارتقای این نمونه" busy={c.busy} onClose={() => c.setPendingUpgrade(null)} onConfirm={c.confirmUpgradeInstance}/><ConfirmDialog open={Boolean(c.confirmDeleteVersion)} title="حذف نسخه کامپوننت" message={c.confirmDeleteVersion ? `نسخه ${c.confirmDeleteVersion.version.semantic_version} از «${c.confirmDeleteVersion.component.name}» حذف شود؟ نسخه جاری یا نسخه‌ای که در جریان‌ها استفاده شده باشد قابل حذف نیست.` : ''} confirmLabel="حذف نسخه" danger busy={c.busy} onClose={() => c.setConfirmDeleteVersion(null)} onConfirm={() => { void c.deleteVersionConfirmed(); }}/><ConfirmDialog open={Boolean(c.confirmUngroup)} title="بازگرداندن کامپوننت به نودها" message={c.confirmUngroup ? `کامپوننت «${String(c.confirmUngroup.data?.label || c.confirmUngroup.data?.typeLabel || '')}» در همین جریان به نودهای داخلی تبدیل شود؟ کامپوننت ذخیره‌شده در کتابخانه و استفاده‌های آن در جریان‌های دیگر بدون تغییر می‌ماند.` : ''} confirmLabel="بازگرداندن به نودها" busy={c.busy} onClose={() => c.setConfirmUngroup(null)} onConfirm={() => { if (c.confirmUngroup)
    c.ungroup(c.confirmUngroup); }}/><ConfirmDialog open={Boolean(c.confirmDelete)} title="حذف کامپوننت" message={c.confirmDelete ? `کامپوننت «${c.confirmDelete.name}» برای همیشه حذف شود؟ این کار فقط زمانی مجاز است که هیچ جریان یا نسخه‌ای از آن استفاده نکند.` : ''} confirmLabel="حذف کامپوننت" danger busy={c.busy} onClose={() => c.setConfirmDelete(null)} onConfirm={() => { void c.deleteConfirmed(); }}/></>; }
