import { useMemo } from 'react';
import { createAutosaveSignature, createAutosaveSnapshot } from '../../../../_model/workflowPersistence';
import { normalizeEdgeHandles, workflowOutputSignature, type FlowGraph } from '../../../../_model/graph';
import type { WorkflowPersistenceOptions } from './types';
export function useWorkflowSnapshot(o: WorkflowPersistenceOptions, name: string) { const currentGraph = useMemo<FlowGraph>(() => ({ nodes: o.nodes, edges: normalizeEdgeHandles(o.nodes, o.edges), meta: { datasetId: o.datasetId, targetColumn: o.targetColumn, taskType: o.taskType, analysisBoards: o.serializedBoards, activeAnalysisBoardId: o.activeBoardId } }), [o.activeBoardId, o.datasetId, o.edges, o.nodes, o.serializedBoards, o.targetColumn, o.taskType]); const currentOutputSignature = useMemo(() => workflowOutputSignature(o.nodes, o.edges, o.datasetId, o.targetColumn, o.taskType), [o.datasetId, o.edges, o.nodes, o.targetColumn, o.taskType]); const autosaveSnapshot = useMemo(() => createAutosaveSnapshot({ name, graph: currentGraph as unknown as Record<string, unknown>, projectId: o.projectId }), [currentGraph, name, o.projectId]); const autosaveSignature = useMemo(() => createAutosaveSignature({ name, projectId: o.projectId, nodes: o.nodes, edges: o.edges, datasetId: o.datasetId, targetColumn: o.targetColumn, taskType: o.taskType, activeBoardId: o.activeBoardId, analysisBoards: o.analysisBoardSignature }), [name, o.activeBoardId, o.analysisBoardSignature, o.datasetId, o.edges, o.nodes, o.projectId, o.targetColumn, o.taskType]); return { currentGraph, currentOutputSignature, autosaveSnapshot, autosaveSignature }; }
