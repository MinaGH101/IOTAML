import { useCallback, useMemo, useState } from 'react';
import type { AnalysisBoardItem, AnalysisBoardTab } from '../../../../_model/board';
import { createMainAnalysisBoard, MAIN_ANALYSIS_BOARD_ID } from '../../../../_model/graph';
import type { AnalysisBoardsOptions } from './types';
export function useBoardCollection(o: AnalysisBoardsOptions) { const [boards, setBoards] = useState<AnalysisBoardTab[]>(() => [createMainAnalysisBoard()]); const [activeBoardId, setActiveBoardId] = useState(MAIN_ANALYSIS_BOARD_ID); const [targetBoardId, setTargetBoardId] = useState(MAIN_ANALYSIS_BOARD_ID); const activeBoard = useMemo(() => boards.find((b) => b.id === activeBoardId) || boards[0], [activeBoardId, boards]); const updateBoardItems = useCallback((id: string, updater: (items: AnalysisBoardItem[]) => AnalysisBoardItem[]) => setBoards((current) => current.map((b) => b.id === id ? { ...b, items: updater(b.items) } : b)), []); const restoreBoards = useCallback((next: AnalysisBoardTab[], requested: string) => { const id = next.some((b) => b.id === requested) ? requested : MAIN_ANALYSIS_BOARD_ID; setBoards(next); setActiveBoardId(id); setTargetBoardId(id); }, []); const selectBoard = useCallback((id: string) => { setActiveBoardId(id); setTargetBoardId(id); }, []); const createBoard = useCallback(() => { if (o.readOnly)
    return; const id = `analysis-board-${crypto.randomUUID()}`; setBoards((current) => [...current, { id, name: `برد ${current.length + 1}`, items: [], viewport: { x: 0, y: 0, scale: 1 }, createdAt: new Date().toISOString() }]); setActiveBoardId(id); setTargetBoardId(id); }, [o.readOnly]); const renameBoard = useCallback((id: string, name: string) => { if (o.readOnly || !name.trim())
    return; setBoards((c) => c.map((b) => b.id === id ? { ...b, name: name.trim() } : b)); }, [o.readOnly]); const removeBoard = useCallback((id: string) => { if (o.readOnly || id === MAIN_ANALYSIS_BOARD_ID)
    return; setBoards((c) => c.filter((b) => b.id !== id)); setActiveBoardId(MAIN_ANALYSIS_BOARD_ID); setTargetBoardId(MAIN_ANALYSIS_BOARD_ID); }, [o.readOnly]); return { boards, setBoards, activeBoard, activeBoardId, targetBoardId, setActiveBoardId, setTargetBoardId, updateBoardItems, restoreBoards, selectBoard, createBoard, renameBoard, removeBoard }; }
