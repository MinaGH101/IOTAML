# Entities

Shared domain entities are introduced here only when more than one feature owns the same stable model. Transport DTOs remain under `shared/_types`; output references currently live with the results feature because it owns their lifecycle.
